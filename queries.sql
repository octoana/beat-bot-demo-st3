-- // create a connection to a mysql database named "stocks"
-- // create a table named "stock" with the following columns:
-- // id: an auto-incrementing integer that serves as the primary key
-- // symbol: a string that represents the stock symbol
-- // price: a float that represents the stock price
-- // quantity: an integer that represents the number of shares

-- // create a table named "users" with the following columns:
-- // id: an auto-incrementing integer that serves as the primary key
-- // username: a string that represents the username
-- // password: a string that represents the password
-- // email: a string that represents the email address

-- write a query that selects all columns from the "stock" table
SELECT * FROM stock;